class System:
    # Class Variable
    
    
    # Constructor
    def _init_(self, name, description, location, router, switch, room, testPlan, archiveStatus):
        self._name = name
        self._description = description
        self._location = location
        self._router = router
        self._switch = switch
        self._room = room
        self._testPlan = testPlan
        self._archiveStatus = archiveStatus

    #name, description, location, router, switch, room, test plan, and archive status