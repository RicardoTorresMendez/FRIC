class System:
    # Class Variable
    
    
    # Constructor
    def __init__(self, name, description, location, router, switch, room, testPlan, archiveStatus):
        self.name = name
        self.description = description
        self.location = location
        self.router = router
        self.switch = switch
        self.room = room
        self.testPlan = testPlan
        self.archiveStatus = archiveStatus

    #name, description, location, router, switch, room, test plan, and archive status
if __name__ == "__main__":
    print('hi')